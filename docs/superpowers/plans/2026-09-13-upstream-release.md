# Upstream Sync and Windows Release Plan

**Goal:** Merge upstream/dev at 95daf90670 into the customized checkout and produce a Windows x64 release installer for 1.18.30.

**Architecture:** Preserve the existing Electron main/preload/renderer boundary and bundled Node server. Merge upstream history without dropping local UI customizations; package the production desktop application with Electron Builder and NSIS.

**Constraints:** Preserve untracked docs/smith files; retain a backup Git ref; do not publish binaries or install/restart the application. Run tests and typechecks from package directories. Synchronize origin/dev after successful validation.

## Execution

- [x] Record baseline regression tests and backup the current commit and untracked documents.
- [x] Merge upstream/dev on smith-agent; reconcile bun.lock with merged package manifests and run bun install --frozen-lockfile.
- [x] Run desktop, app and session-ui typechecks and focused regression tests for desktop packaging, sidebar layout, file references and Mermaid. Inspect the automatic timeline merge for preservation of local customizations.
- [x] Build packages/desktop with OPENCODE_CHANNEL=prod and OPENCODE_VERSION=1.18.30 using bun run build.
- [x] Run bun run package:win -- --x64 --publish never with the validated local Electron distribution if download fallback is needed.
- [x] Verify installer metadata, packaged server version, native dependencies and SHA-256; preserve a versioned release copy.
Final integration: commit the validated merge, fast-forward local dev, push dev to origin, and record the verified remote revision in the release manifest.

## Validation commands

From packages/desktop: `bun test electron-builder.config.test.ts`, `bun typecheck`.
From packages/app: `bun test --conditions=solid --preload ./happydom.ts ./src/context/layout.test.ts ./src/pages/session/session-panel-layout.test.ts ./src/pages/layout/sidebar-collapse.test.ts ./src/pages/session/markdown-file-reference.test.tsx`, `bun typecheck`.
From packages/session-ui: `bun test src/components/markdown-file-reference.test.ts src/components/markdown-mermaid.test.ts`, `bun typecheck`.

The installer must identify as OpenCode 1.18.30 and be generated by a successful NSIS packaging invocation. Record signing status accurately; local packaging does not supply the upstream signing certificate.

## Verified results

- Typechecks passed for desktop, app, session-ui and opencode.
- 817 regression tests passed (7 desktop, 22 app, 56 session-ui, 732 backend).
- Production Electron Vite build and Windows x64 NSIS packaging completed successfully.
- Archive validation matched 9 JS/HTML/WASM files against build outputs; Electron 42.3.3 native PTY command and watcher load passed.
- Installer metadata: OpenCode 1.18.30, 134769625 bytes, locally unsigned.
- SHA-256: 73F0B21EAF748E9F3EA75EB84048EE637BC6035134F3649794187CEC282D57C9.
- Preserved all five untracked Smith files byte-for-byte. Backup branch: backup-before-sync.
- Installation maintenance: quarantined two stale local OpenAI SDK directories; refreshed dependencies without native install scripts because tree-sitter-powershell attempted an unavailable C++ toolchain. Verified bundled WASM parser assets and the actual packaged Windows native modules.
- Independent merge review found no critical or important issues. Upstream patch context whitespace was retained intact.
